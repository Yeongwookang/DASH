<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="stationmanage">
	 <select id="listStation" resultType="com.sp.app.stationmanage.Stationmanage">
	 	select stNum, x_pos, y_pos, name, maxQty, zoonname, addr ,  IMAGEFILENAME from station 
	 </select>
</mapper>